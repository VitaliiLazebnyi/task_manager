<tr>
  <td><%= project.title %></td>
  <td><%= link_to 'Edit', edit_project_path(project) %></td>
  <td><%= link_to 'Remove', [project], method: :delete, data: { confirm: 'Please confirm. Are you sure?' } %></td>
</tr>
